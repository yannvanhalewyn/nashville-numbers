/** @jsx React.DOM */

console.log("Working on stuff");

/*
 * var React = require('react');
 * var Bar = require('./Components/bar.react');
 * 
 * React.render(
 *   <Bar/>,
 *   document.body
 * );
 */
